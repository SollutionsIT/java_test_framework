# Copy to .env. JVM -D options override environment variables, then .env, then defaults.
BASE_URL=http://127.0.0.1:8080
# false when testing an already running environment with the same API/UI contract
# quick setup for local development, or when running in CI/CD pipelines
DEMO_MODE=true
HEADLESS=true
TIMEOUT_MS=10000
